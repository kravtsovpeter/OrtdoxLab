<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>
</head>
<body>
	<div>
		<form action="/logout" method="post">
            <input type="hidden" name="_csrf" value="{{_csrf.token}}" />
            <input type="submit" value="Sign Out"/>
        </form>
	</div>
	<div>
		<form method="post">
			<input type="text" name="place" placeholder="Введите место встречи" />
			<input type="text" name="owner" placeholder="Введите автора встречи" />
			<input type="datetime" name="date" placeholder="Ввeдите дату встречи" />
			<input type="hidden" name="_csrf" value="{{_csrf.token}}" />
			<button type="submit">Добавить</button>
		</form>
	</div>
	<div>Список встреч</div>
	<form method="post" action="filter">
		<input type="text" name="filter" />
		<input type="hidden" name="_csrf" value="{{_csrf.token}}" />
		<button type="submit">Найти</button>
	</form>
	{{#meetings}}
	<div>
		<b>{{id}}</b>
		<span>{{place}}</span>
		<i>{{owner}}</i>
		<b>{{formatedDate}}</b>
	</div>
	{{/meetings}}
</body>
</html>